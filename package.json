{
  "name": "jeyson-mcp",
  "version": "1.0.0",
  "description": "MCP server for Jeyson API — lets AI coding assistants list schemas, call endpoints, and generate mobile/web integration code.",
  "type": "module",
  "files": [
    "dist",
    "README.md"
  ],
  "bin": {
    "jeyson-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build",
    "start": "node dist/index.js",
    "dev": "npx tsx src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.0.0"
  }
}
